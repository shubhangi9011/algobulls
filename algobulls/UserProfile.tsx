// src/components/MyProfile/UserProfile.tsx
import React from 'react';
import { Card } from 'antd';

const UserProfile: React.FC = ({ user }: { user: any }) => {
  return (
    <div>
      <Card>
        {/* User profile details */}
      </Card>
    </div>
  );
};

export default UserProfile;
